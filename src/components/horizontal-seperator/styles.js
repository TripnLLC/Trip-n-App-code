import { StyleSheet } from 'react-native';
import { colors, fonts, spacing } from '../../theme';
import { w, h } from '../../theme';

export default styles = StyleSheet.create({
  container: {
    backgroundColor: colors.palette.lightGreen,
    width: '70%',
    height: 1,
    marginVertical: w(16),
    //marginHorizontal: w(32)
  },
});
